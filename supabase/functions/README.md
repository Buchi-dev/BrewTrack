# Supabase Functions

Put Supabase Edge Functions in this folder.

Each function gets its own folder:

```text
supabase/functions/<function-name>/index.ts
```

The `hello` folder is a starter function. Add your next function beside it.
